# IDB 项目颜色使用规范

## 🎨 概述

本文档规定了 IDB 项目中颜色的使用规范，确保整个项目的颜色系统统一、可维护，并支持完整的深色模式。

## 📋 基本原则

### ✅ 必须遵守
- **禁止硬编码颜色值**：不允许在代码中直接使用 `#ffffff`、`rgba(0,0,0,0.5)` 等硬编码颜色
- **统一使用CSS变量**：所有颜色必须通过 `var(--variable-name)` 的方式引用
- **遵循语义化命名**：使用有意义的变量名，如 `--color-text-1` 而不是 `--gray-dark`

### ❌ 禁止行为
- 在组件中硬编码颜色值
- 在样式文件中直接使用十六进制颜色
- 使用内联样式设置颜色
- 绕过CSS变量系统

### 🎯 例外情况（允许硬编码）
- **CSS变量定义**：在 `util.less` 中定义品牌色的十六进制值（这是变量的源定义）
- **SVG图标**：图标文件中的固有颜色
- **配置文件**：如 `settings.json` 中用户可配置的主题色
- **最终回退值**：在多层回退机制的最后一层，如 `#cccccc`（但应优先使用CSS变量）

## 🏗️ 颜色系统架构

### 1. 系统级颜色（主题包提供）
用于基础UI组件的颜色，支持自动的深色模式切换。

#### 背景色系统
```css
var(--color-bg-1)    /* 主背景 - 页面背景 */
var(--color-bg-2)    /* 次背景 - 卡片、面板背景 */
var(--color-bg-3)    /* 三级背景 - 输入框、按钮背景 */
var(--color-bg-4)    /* 四级背景 - 更深层级背景 */
var(--color-bg-5)    /* 浮层背景 - 模态框、下拉菜单 */
```

#### 文字色系统
```css
var(--color-text-1)  /* 主文字 - 标题、重要文字 */
var(--color-text-2)  /* 次文字 - 正文、描述文字 */
var(--color-text-3)  /* 辅助文字 - 提示、说明文字 */
var(--color-text-4)  /* 禁用文字 - 禁用状态文字 */
```

#### 边框色系统
```css
var(--color-border-1) /* 主边框 - 输入框、卡片边框 */
var(--color-border-2) /* 次边框 - 分割线、表格边框 */
var(--color-border-3) /* 强调边框 - 激活状态边框 */
var(--color-border-4) /* 重点边框 - 重点强调边框 */
```

#### 填充色系统
```css
var(--color-fill-1)  /* 浅填充 - 最浅填充色 */
var(--color-fill-2)  /* 次填充 - 次级填充色 */
var(--color-fill-3)  /* 中填充 - 中等填充色 */
var(--color-fill-4)  /* 深填充 - 较深填充色 */
```

### 2. 业务级颜色（品牌色系统）
用于业务特色和品牌识别的颜色。

#### IDB 品牌色
```css
/* 碧海蓝 - 主品牌色 */
var(--idblue-6)      /* 主色调 */
var(--idblue-5)      /* 浅色变体 */
var(--idblue-4)      /* 深色变体 */
var(--idblue-1)      /* 最浅背景色 */

/* 新生绿 - 成功色 */
var(--idbgreen-6)    /* 主色调 */
var(--idbgreen-5)    /* 浅色变体 */
var(--idbgreen-4)    /* 深色变体 */
var(--idbgreen-1)    /* 最浅背景色 */

/* 警示红 - 错误色 */
var(--idbred-6)      /* 主色调 */
var(--idbred-5)      /* 浅色变体 */
var(--idbred-4)      /* 深色变体 */
var(--idbred-1)      /* 最浅背景色 */

/* 晚秋红 - 警告色 */
var(--idbautumn-6)   /* 主色调 */

/* 黄昏 - 提示色 */
var(--idbdusk-6)     /* 主色调 */

/* 碧涛青 - 信息色 */
var(--idbturquoise-6) /* 主色调 */

/* 品牌特色颜色 */
var(--idb-brand-bg)   /* 品牌背景色 - 登录页面等特色区域 */
var(--idb-brand-text) /* 品牌文字色 - 配合品牌背景使用 */
var(--idb-fallback-gray) /* 回退灰色 - 工具函数默认色 */

/* 透明度和阴影颜色 */
var(--idb-shadow-light)   /* 浅阴影 - 轻微阴影效果 */
var(--idb-shadow-medium)  /* 中等阴影 - 标准阴影效果 */
var(--idb-shadow-dark)    /* 深阴影 - 强烈阴影效果 */
var(--idb-overlay-light)  /* 浅遮罩 - 轻微遮罩效果 */
var(--idb-overlay-medium) /* 中等遮罩 - 标准遮罩效果 */
var(--idb-overlay-dark)   /* 深遮罩 - 强烈遮罩效果 */
```

## 🎯 使用场景指南

### 系统级颜色使用场景

#### 背景色
```css
/* ✅ 正确使用 */
.card {
  background: var(--color-bg-2);  /* 卡片背景 */
}

.modal {
  background: var(--color-bg-5);  /* 模态框背景 */
}

/* ❌ 错误使用 */
.card {
  background: #ffffff;  /* 硬编码 */
}
```

#### 文字色
```css
/* ✅ 正确使用 */
.title {
  color: var(--color-text-1);     /* 标题文字 */
}

.description {
  color: var(--color-text-2);     /* 描述文字 */
}

/* ❌ 错误使用 */
.title {
  color: #1d2129;  /* 硬编码 */
}
```

### 业务级颜色使用场景

#### 品牌色
```css
/* ✅ 正确使用 */
.primary-button {
  background: var(--idblue-6);    /* 主按钮 */
}

.success-badge {
  background: var(--idbgreen-6);  /* 成功徽章 */
}

.error-message {
  color: var(--idbred-6);         /* 错误信息 */
}

/* ❌ 错误使用 */
.primary-button {
  background: #3491fa;  /* 硬编码品牌色 */
}
```

## 🔧 特殊场景处理

### 1. JavaScript 中使用颜色
```typescript
// ✅ 正确方式 - 获取CSS变量值
const getCSSVar = (varName: string) => {
  if (typeof window !== 'undefined') {
    return getComputedStyle(document.documentElement)
      .getPropertyValue(varName).trim();
  }
  return '';
};

const primaryColor = getCSSVar('--idblue-6');

// ❌ 错误方式 - 硬编码
const primaryColor = '#3491fa';
```

### 2. 动态颜色生成
```typescript
// ✅ 正确方式 - 基于CSS变量生成
const getViewModeColor = (mode: string): string => {
  switch (mode) {
    case 'success':
      return getCSSVar('--idbgreen-6') || '#00B42A';  // 提供回退值
    case 'error':
      return getCSSVar('--idbred-6') || '#F53F3F';
    default:
      return getCSSVar('--color-text-3') || '#86909C';
  }
};
```

### 3. 第三方组件主题适配
```typescript
// ✅ 正确方式 - 终端组件主题
const terminalTheme = computed(() => {
  return {
    background: 'var(--color-bg-1)',
    foreground: 'var(--color-text-1)',
    cursor: 'var(--color-text-1)',
    // ANSI 颜色使用品牌色
    red: 'var(--idbred-6)',
    green: 'var(--idbgreen-6)',
    blue: 'var(--idblue-6)',
  };
});
```

## 📝 代码审查清单

### 提交代码前检查
- [ ] 没有硬编码的十六进制颜色值（#ffffff、#000000等）
- [ ] 没有硬编码的RGB/RGBA值（rgb(255,255,255)、rgba(0,0,0,0.5)等）
- [ ] 所有颜色都使用了CSS变量（var(--variable-name)）
- [ ] 使用了语义化的变量名
- [ ] 新增的颜色变量已添加到相应的CSS文件中

### 常见问题检查
- [ ] 组件样式中是否有 `background: #fff`
- [ ] JavaScript中是否有硬编码颜色字符串
- [ ] 内联样式中是否有颜色值
- [ ] 第三方组件配置中是否有硬编码颜色

## 🚀 最佳实践

### 1. 新增颜色变量
如需新增颜色，应在 `frontend/src/assets/style/util.less` 中定义：

```less
:root {
  // 新增品牌色
  --idb-new-color-6: #your-color;
  --idb-new-color-5: #lighter-variant;
  --idb-new-color-4: #darker-variant;
}
```

### 2. 深色模式适配
新增的颜色应考虑深色模式适配：

```less
// 亮色模式
:root {
  --custom-bg: #ffffff;
}

// 深色模式
body[arco-theme='dark'] {
  --custom-bg: #1a1a1a;
}
```

### 3. 组件开发
```vue
<template>
  <div class="my-component">
    <h1 class="title">标题</h1>
    <p class="content">内容</p>
  </div>
</template>

<style scoped>
.my-component {
  background: var(--color-bg-2);
  border: 1px solid var(--color-border-2);
}

.title {
  color: var(--color-text-1);
}

.content {
  color: var(--color-text-2);
}
</style>
```

## 📚 参考资源

- [Arco Design 主题定制](https://arco.design/docs/designlab/use-theme-package)
- [CSS 自定义属性](https://developer.mozilla.org/zh-CN/docs/Web/CSS/--*)
- [深色模式设计指南](https://arco.design/react/docs/dark)

---

**记住：统一的颜色系统是优秀用户体验的基础！** 🎨
