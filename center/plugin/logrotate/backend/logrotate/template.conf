${iDB_logrotate_log_path} {
	${iDB_logrotate_frequency}
	rotate ${iDB_logrotate_count}
	${iDB_logrotate_compress}
	${iDB_logrotate_delay_compress}
	create ${iDB_logrotate_create_mode} ${iDB_logrotate_create_owner} ${iDB_logrotate_create_group}
	${iDB_logrotate_missing_ok}
	${iDB_logrotate_notif_empty}
	prerotate
		${iDB_logrotate_pre}
	endscript
	postrotate
		${iDB_logrotate_post}
	endscript
}